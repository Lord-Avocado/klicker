var score = 0; // initializing score variable


function click() {
  // onclick function - will add 1 to score, and for now, to test, will console.log("Click " + score + "!")
  score += 1;
  // for testing purposes
  console.log("Click " + score + "!");
}
